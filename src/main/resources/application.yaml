spring.application:
  name: oris_spring

logging:
  enabled: true

info:
  app:
    name: Learn Spring
    version: 0.2.0

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application: Learn Spring
  datasource:
    url: 'jdbc:postgresql://localhost:5432/spring'
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver